<!DOCTYPE html>
<html>
<head>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
      padding: 30px;
      background: white;
      color: black;
    }

    .header {
      text-align: center;
      margin-bottom: 30px;
    }

    h1 {
      font-size: 42px;
      font-weight: bold;
      margin-bottom: 8px;
    }

    .subtitle {
      font-size: 16px;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .now-playing {
      background: black;
      color: white;
      padding: 20px;
      margin-bottom: 30px;
      text-align: center;
    }

    .now-playing-label {
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: 10px;
    }

    .now-playing-track {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 5px;
    }

    .now-playing-artist {
      font-size: 18px;
    }

    .top-list {
      margin-bottom: 25px;
    }

    .list-title {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 15px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .list-item {
      font-size: 18px;
      padding: 12px 0;
      border-bottom: 1px solid #ccc;
      display: flex;
      align-items: baseline;
    }

    .list-item:last-child {
      border-bottom: none;
    }

    .rank {
      font-weight: bold;
      margin-right: 15px;
      min-width: 30px;
      font-size: 20px;
    }

    .item-name {
      font-weight: 600;
    }

    .item-details {
      font-size: 14px;
      color: #555;
      margin-left: 8px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>🎵 My Spotify</h1>
    <div class="subtitle">{{ time_range_label }}</div>
  </div>

  {% if currently_playing %}
  <div class="now-playing">
    <div class="now-playing-label">▶ Now Playing</div>
    <div class="now-playing-track">{{ currently_playing.name }}</div>
    <div class="now-playing-artist">{{ currently_playing.artist }}</div>
  </div>
  {% endif %}

  <div class="top-list">
    <div class="list-title">Top Artists</div>
    {% for artist in top_artists limit:3 %}
    <div class="list-item">
      <span class="rank">{{ artist.rank }}.</span>
      <span class="item-name">{{ artist.name }}</span>
    </div>
    {% endfor %}
  </div>

  <div class="top-list">
    <div class="list-title">Top Tracks</div>
    {% for track in top_tracks limit:3 %}
    <div class="list-item">
      <span class="rank">{{ track.rank }}.</span>
      <span class="item-name">{{ track.name }}</span>
      <span class="item-details">— {{ track.artist }}</span>
    </div>
    {% endfor %}
  </div>
</body>
</html>
